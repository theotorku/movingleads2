name: my first workflow

on: push

jobs:
    firs_job:
        runs-on: ubuntu-latest
        steps:
            - name: welcome message
              run: echo "my first github action job"

            - name: list file
              run: ls

            - name: read file
              run: cat README.md